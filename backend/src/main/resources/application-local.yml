spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/funeat
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.hibernate.type.descriptor.sql: trace

kakao:
  rest-api-key: 1234
  redirect-uri: 1234
  admin-key: 1234

cloud:
  aws:
    region:
      static: testRegion
    s3:
      bucket: testBucket
      folder: testFolder
      cloudfrontPath: 11
      expiration:
        time: 1111
