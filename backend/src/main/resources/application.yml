spring:
  profiles:
    active: { DEPLOY_ACTIVE:dev }

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

springdoc:
  swagger-ui:
    path: /funeat-api
    enabled: true
    tags-sorter: alpha
